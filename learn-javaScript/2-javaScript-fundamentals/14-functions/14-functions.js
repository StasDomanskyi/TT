/* Task 1 - no coding */

/* Task 2 */

let age = 15;

// Two variants of function needed in task

/* function checkAge(age) {
  return age > 18 ? true : confirm('Did parents allow you?');
} */

function checkAge(age) {
  return age > 18 || confirm('Did parents allow you?');
} 

checkAge(age);

/* Task 3 */

function min(a, b) {
  return a < b ? a : b;
}

min(2, 5);

/* Task 4 in 4-task.html */

